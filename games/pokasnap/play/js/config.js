/* Owner-supplied settings that are not code. */
export const CONFIG = {
  // Official KEEPITIL / PokaSnap SoundCloud URL. While null, the MUSIC section
  // shows only the built-in soundtrack toggle -- no guessed link to someone
  // else's profile is ever shipped.
  soundcloudUrl: null,
  soundcloudTrackUrl: null,
};
